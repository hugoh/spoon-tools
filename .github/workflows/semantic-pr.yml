---
name: semantic-pr

on: # yamllint disable-line rule:truthy
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions: {}

jobs:
  semantic-pr:
    uses: hugoh/gh-workflows/.github/workflows/semantic-pr.yml@ae1366a14513d42c78dd993d7fbb776fda2d4530 # v1.38.6
    permissions:
      pull-requests: read
