---
name: semantic-pr

on: # yamllint disable-line rule:truthy
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions: {}

jobs:
  semantic-pr:
    uses: hugoh/gh-workflows/.github/workflows/semantic-pr.yml@0e87e19c850cf07d9e32b37416a6b60ab6a666dd # v1.38.4
    permissions:
      pull-requests: read
