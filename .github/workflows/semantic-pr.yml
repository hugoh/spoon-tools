---
name: semantic-pr

on: # yamllint disable-line rule:truthy
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions: {}

jobs:
  semantic-pr:
    uses: hugoh/gh-workflows/.github/workflows/semantic-pr.yml@b95b3bd62a98e3c18adbd6ed7e22f50908a50467 # v1.40.1
    permissions:
      pull-requests: read
