---
name: semantic-pr

on: # yamllint disable-line rule:truthy
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions: {}

jobs:
  semantic-pr:
    uses: hugoh/gh-workflows/.github/workflows/semantic-pr.yml@98894bfdb331f6bfc3e1b7a1d1ddbe637080a031 # v1.40.4
    permissions:
      pull-requests: read
