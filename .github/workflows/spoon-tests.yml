on:
  workflow_call:
    inputs:
      pre_test:
        description: Command to run before tests (e.g. "mise run vendor")
        type: string
        required: false
        default: ""

permissions:
  contents: read

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    concurrency:
      group: ${{ github.workflow }}-${{ github.job }}-${{ github.event.pull_request.number || github.ref }}
      cancel-in-progress: true
    permissions:
      contents: read
    steps:
      - uses: hugoh/gh-workflows/setup@0605171371448c645486e3200e1940b1813ef9f1 # v1.12.0
      - name: Pre-test
        if: inputs.pre_test != ''
        env:
          PRE_TEST_CMD: ${{ inputs.pre_test }}
        run: $PRE_TEST_CMD
      - name: Run tests
        run: mise run test
